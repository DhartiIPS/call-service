export enum CallStatus {
  INITIATED = 'initiated',
  RINGING = 'ringing',
  CONNECTED = 'connected',
  ENDED = 'ended',
  REJECTED = 'rejected',
  ACCEPTED = 'accepted',
  MISSED = 'missed',
  CANCELLED = 'cancelled',
}